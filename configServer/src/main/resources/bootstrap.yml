server:
  port: 8096
spring:
  cloud:
    config:
      server:
        git:
        # url del repo
          uri: https://github.com/WillyNTTDATA/bootCampConfigService.git
          # ruta donde esta el archivo yml
          searchPaths: config-data
       #  username: 
       #  password: